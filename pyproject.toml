[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ceco-lad"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.4.0",
    "numpy>=1.24",
    "scipy>=1.10",
    "scikit-learn>=1.3",
    "pandas>=1.5",
    "pyyaml>=6.0",
    "tqdm>=4.65",
    "gdown>=6.0",
]

[project.optional-dependencies]
dashboard = [
    "fastapi>=0.100",
    "uvicorn[standard]>=0.20",
]
dev = [
    "pytest>=7.0",
]

[project.scripts]
ceco-download = "tools.download_checkpoints:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["ceco_core*", "training_pipeline*", "ceco_lad_inference_pipeline*", "quantization*", "tools*", "dashboard*"]
